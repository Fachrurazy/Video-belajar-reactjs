const labelh2 = (props) => {
    const { children } = props
    return (
        <h2 className="mb-6 text-sm font-semibold text-gray-900 uppercase">
            {children}
        </h2>
    )
}

export default labelh2